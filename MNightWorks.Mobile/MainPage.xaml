<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MNightWorks.Mobile.MainPage">

    <!-- ScrollView lets the page scroll if the menu is longer than the screen -->
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="15">

            <Label Text="Our Menu" FontSize="32" FontAttributes="Bold" />

            <!-- CollectionView is MAUI's control for showing a repeating list —
                 the XAML equivalent of the .map() loop that built HTML cards in script.js -->
            <CollectionView x:Name="MenuCollectionView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <!-- This layout repeats once per item, same idea as the
                             template literal that ran once per menu item in JS -->
                        <Frame Padding="12" Margin="0,0,0,10" CornerRadius="8" BorderColor="LightGray">
                            <VerticalStackLayout Spacing="4">
                                <!-- {Binding Name} pulls the Name property straight off
                                     whichever MenuItem this card is currently showing -->
                                <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                                <Label Text="{Binding Description}" FontSize="14" TextColor="Gray" />
                                <Label Text="{Binding Price, StringFormat='R{0:F2}'}"
                                       FontSize="16" TextColor="Green" FontAttributes="Bold" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>